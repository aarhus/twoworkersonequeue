{
  "name": "producer",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/index.js",
  "private": true,
  "module": "dist/index.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild --bundle --format=esm --sourcemap --outfile=dist/index.mjs ./src/index.ts",
    "dev": "miniflare --watch --debug"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230404.0",
    "esbuild": "^0.17.15",
    "miniflare": "^2.13.0",
    "typescript": "^5.0.4",
    "wrangler": "^2.14.0"
  }
}
